千束，已经同步到服务器了。

现在打开：

```text
http://39.106.125.149/
```

会先出现登录页。

账号密码是：

```text
用户名：chisado
密码：chisado233
```

这次改了两块：

1. **网页端加了访问门禁**
   - 未登录会停在登录页
   - 登录成功后会记在浏览器 `localStorage`，下次同一浏览器不用重复登
   - 注意：这是前端门禁，能挡普通访问，但不是强安全认证。以后要更安全，需要后端鉴权或 Nginx Basic Auth。

2. **修复“单个会话还没法输入/发送”的问题**
   - 之前输入框依赖 `activeConversationId`
   - 如果只有一个会话、或者还没点进会话时，发送按钮/输入框可能被禁用
   - 我加了 `effectiveConversationId`，会自动用当前会话或第一个会话，兜底到 `primary`
   - 发送时也会自动绑定当前可用会话

我已经完成：

```text
pnpm --filter @chat-soft/mobile build
```

并把新的构建产物上传部署到：

```text
/www/wwwroot/chat-soft
```

服务器上的前端服务 `chat-soft-web` 也已经重启。

验证结果：

- `http://39.106.125.149/` 返回 `200`
- 新 bundle 已上线
- bundle 内确认包含登录逻辑、`chisado`、`chisado233`

你现在可以手机刷新页面试一下。若之前已经打开过旧页面，建议 Safari 里强刷/关掉标签重新打开。