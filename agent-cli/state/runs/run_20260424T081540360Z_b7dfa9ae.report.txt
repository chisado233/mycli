正在补。你刚才这句“补全你的 skill 和 readme 之类的”，我理解为要把 `project-manager` 的**文档体系彻底补齐**，不只是命令能跑，还要让后续 agent/人类能通过文档和 `--help` 明确知道怎么用、怎么维护、怎么接手项目。

我准备补这几类：

## 1. `SKILL.md`

路径：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\SKILL.md
```

会补强：

- 什么时候必须用 project-manager；
- 什么时候不用；
- agent 接手项目标准流程；
- 动态上下文的录入规则；
- task / next-action 的区别；
- ongoing 项目维护规则；
- 修改文档/命令后如何同步到 mycli 和 skill-library；
- 验证命令。

---

## 2. `README.md`

路径：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\README.md
```

会补强：

- 总体架构；
- `.agent-project/` 每个目录的职责；
- `project.json` 字段说明；
- `registry.json` 说明；
- task/next-action/context/decision/operation 的结构；
- 所有命令分组说明；
- 有限项目、持续项目、agent 接手、维护场景的完整示例；
- 设计原则和边界。

---

## 3. `USER_MANUAL.md`

路径：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\USER_MANUAL.md
```

现在还比较薄，会补成用户手册，包括：

- 快速开始；
- 新建项目；
- 查询项目；
- 更新状态；
- 添加 task；
- 添加 next-action；
- 添加阶段上下文；
- agent 接手项目；
- 维护 ongoing 项目；
- 常见问题；
- 推荐命令顺序。

---

## 4. `registration/mycli/README.md`

这个就是：

```powershell
mycli project-manager --help
```

的来源。

路径：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\registration\mycli\README.md
```

会补：

- 命令完整速查；
- 典型工作流；
- 常见查询；
- 状态字段；
- 存储路径；
- examples。

---

## 5. schema / templates

目前 schema 已有：

```text
schemas/
  project.schema.json
  registry.schema.json
  task.schema.json
  next-action.schema.json
  operation.schema.json
```

我准备补：

```text
context.schema.json
```

并补模板：

```text
templates/context.json
templates/agent-guide.md
templates/maintenance-guide.md
```

这样动态上下文、agent 接手、维护准则都有对应模板。

---

## 6. 同步与验证

补完后会跑：

```powershell
D:\agent_workspace\capability-library\agent-system-rules\project-manager\scripts\register-to-mycli.ps1
```

然后验证：

```powershell
mycli project-manager --help
mycli project-manager list
mycli project-manager agent-guide project-manager
mycli project-manager maintenance-guide project-manager
mycli skill-library search project-manager
python -m py_compile D:\agent_workspace\capability-library\agent-system-rules\project-manager\src\project_manager.py
```

最后我会把 `project-manager` 自己的项目状态也更新一下。

我继续做。