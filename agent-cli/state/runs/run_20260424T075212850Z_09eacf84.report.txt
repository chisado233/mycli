千束，已经全部补完并同步验证了。

## 已补全内容

### 1. 补全了 `project-manager` 源文档

更新了：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\README.md
```

补充内容包括：

- 完整命令说明；
- `init/register/scan/refresh` 说明；
- `query` 支持的所有过滤字段；
- `update-status` 用法；
- `task-list/task-add/task-update` 用法；
- `next-list/next-add/next-update` 用法；
- 有限项目和持续项目的典型使用流程；
- 旧 `project-skill-manager` 状态迁移说明；
- 验证命令；
- 后续增强方向。

---

### 2. 补全了 `SKILL.md`

更新了：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\SKILL.md
```

补充内容包括：

- 使用场景；
- 命令速查；
- 维护项目自身的同步流程；
- `finite / ongoing` 说明；
- ongoing 项目的规则；
- 状态轴说明；
- 修改 help 或 command 后如何同步到 mycli 和 skill-library。

---

### 3. 补全了 `mycli project-manager --help`

更新了源注册文档：

```text
D:\agent_workspace\capability-library\agent-system-rules\project-manager\registration\mycli\README.md
```

并同步到了：

```text
D:\agent_workspace\capability-library\mycli\project-manager\README.md
```

现在执行：

```powershell
mycli project-manager --help
```

可以看到：

- 命令清单；
- command details；
- 初始化、查询、状态维护、task/next-action 维护示例；
- 存储位置；
- ongoing 项目规则；
- registry 说明。

---

### 4. 写入了我的 `private-assistant.md`

已更新：

```text
C:\Users\38188\.config\opencode\agent\private-assistant.md
```

新增了 `mycli project-manager` 使用规则，包括：

- 用途；
- 常用命令；
- 什么时候优先使用；
- `project.json` 事实来源；
- registry 路径；
- `finite / ongoing` 区别；
- ongoing 不用 `completed`，而用 `retired`；
- project-manager / agent-workflow / agent-cli 的边界。

以后我会在跨会话、多阶段、持续性项目里优先想到：

```powershell
mycli project-manager ...
```

---

## 已同步注册

执行了：

```powershell
D:\agent_workspace\capability-library\agent-system-rules\project-manager\scripts\register-to-mycli.ps1
```

同步到了：

```text
D:\agent_workspace\capability-library\mycli\project-manager\
D:\agent_workspace\capability-library\skill-library\project-manager\
```

---

## 已验证

验证通过：

```powershell
python -m py_compile D:\agent_workspace\capability-library\agent-system-rules\project-manager\src\project_manager.py
```

通过。

```powershell
mycli project-manager --help
```

通过，显示新 help。

```powershell
mycli project-manager list
```

通过，显示完整命令列表，包括：

```text
update-status
task-list
task-add
task-update
next-list
next-add
next-update
```

```powershell
mycli skill-library search project-manager
```

通过，能找到 skill。

---

## 已更新 project-manager 自己的项目状态

执行了：

```powershell
mycli project-manager update-status project-manager ...
```

当前状态已经更新为：

```text
lifecycle: active
phase: delivery
activity: stable
health: green
delivery: published
summary: Project-manager docs, SKILL, mycli help, and private-assistant guidance are updated.
```

---

现在 `project-manager` 已经是：

```text
可发现
可 help
可注册项目
可查询项目
可维护状态
可管理 task
可管理 next-action
已写入我的持久提示词
```

后续新项目可以直接用这个包来建档和管理。